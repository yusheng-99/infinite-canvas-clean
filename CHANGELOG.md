# CHANGELOG

## Unreleased

## v0.1.4 - 2026-07-27 - 原项目 v0.10.0

+ [新增] 画布组节点：可将节点拖入组内统一移动管理，拖出后自动脱离，组不参与连线。

+ [新增] 提示词输入支持 @ 引用画布资源，引用以缩略图芯片形式展示，删除更直观。

+ [新增] 生图设置新增透明背景开关，生成和编辑请求会带上背景参数。

+ [新增] 画布左上角菜单新增“导出当前画布”，可将当前画布及引用媒体导出为压缩包。

+ [优化] 生成完成后保留提示词输入内容，方便微调后再次生成。

+ [优化] 首页改为纯启动器：隐藏侧栏、全屏入口卡片，右上角保留快捷操作；进入工作台后侧栏恢复显示。

## v0.1.3 - 2026-07-27

+ [优化] 整体页面布局改为渐变底壳 + 圆角卡片工作区结构，侧栏导航高亮升级为卡片式加主题色指示条。

+ [优化] 全局字体：标题改为现代无衬线字体，等宽字体补充中文回退，提升整体可读性。

+ [优化] 侧栏导航重排为「创作 / 资源」两组分区，文字加大加深、激活项主题色高亮，提升可读性。

## v0.1.2 - 2026-07-24

+ [新增] 画廊模块：本地收藏生成图片，支持瀑布流欣赏、大图预览，以及从画布/生图工作台/素材导入。

+ [优化] 欢迎页改为苹果风现代简约布局，入口卡片支持中心放大悬浮交互。

+ [优化] 顶栏导航居中，欢迎页入口卡片对齐。

+ [优化] 画布、提示词、素材、画廊与生成结果等可选卡片统一中心放大悬浮效果。

+ [优化] 画布底部工具栏固定长度，节点操作按钮未选中时置灰。

## v0.1.1 - 2026-07-23

+ [修复] 画布项目大厅卡片预览图改为从 IndexedDB 深度解析图片 StorageKey，并实现防破图容错与 1/2/3 列自适应布局。

## v0.1.0 - 2026-07-23

+ [新增] 图片和视频模型支持按模型配置自定义调用脚本，可适配不同中转站的请求、参考图和任务轮询格式；未配置脚本时继续使用原有接口。

+ [新增] 移植提示词广场，支持多数据源、搜索、分类、标签、最新、收藏、投稿者主页、提示词变体与参考图预览。

+ [新增] 提示词可直接用于生图工作台、视频创作台和画布生成节点，也可保存为本地文字素材。

+ [新增] 画布图片节点快捷工具栏增加一键投稿，可自动带入图片、标题、提示词和模型信息，编辑投稿人、标签与备注后提交到提示词广场。

+ [修复] 一键投稿图片上传在 Catbox 拒绝匿名上传时自动改用 ImgBB，避免出现 `Invalid uploader`。

+ [新增] 画布右上角增加提示词广场入口，可在浮窗中浏览和筛选提示词，并将选中的提示词直接创建为文本节点。

+ [优化] 全局 UI 与画布主题 Token 统一，顶栏改为更轻的半透明吸顶样式。

+ [优化] 画布项目大厅支持预览图、搜索、多维排序，以及网格/列表视图切换。

+ [优化] 画布底部工具栏改为胶囊 Dock，外观与布局整理收纳到 Popover。

+ [优化] 画布大图预览移除全屏动态模糊和常驻 GPU 图层，避免播放视频时拖慢浏览器与桌面。

+ [优化] 生成图片自动落位和整理统一按生成中节点的固定尺寸计算，避免成图比例不同导致行列错位。

+ [修复] 修正生成中图片节点打开信息弹窗时 JSON 格式化变量未定义导致页面崩溃的问题。

## v0.0.9 - 2026-07-15

+ [新增] 画布空白处支持双击选择并创建节点。

+ [优化] 节点快捷工具栏改为仅在单选节点后显示。

+ [优化] 生成节点自动识别同一配置的多列输出，优先放到最短列下方；各列等高时从第一列开始排列。

+ [优化] 生成配置节点创建图片、视频、音频或文本结果时自动查找右侧下一块空位，新批次按已有输出列向下整齐排列。

+ [修复] 混合布局改为先分列再逐列纵向排列，避免高节点跨行导致上下分组被合并重叠。

+ [修复] 混合方向自动整理统一各列左侧位置和起始顶部，并根据节点尺寸保持纵向间距。

+ [修复] 混合方向自动整理会压缩行列分组之间的过大空白，使右侧节点组紧邻左侧内容排列。

+ [修复] 自动整理不再把同时包含横向与纵向分支的连通结构强制改成单一方向，保留原有空间关系并仅做网格归整。

+ [优化] 自动整理按每组连线原有方向选择横向或纵向布局，并保留向左、向右、向上和向下的排列方向。

+ [修复] 自动整理保持批量图片根节点与子节点的独立分组及双列结构，避免不同批次混排成单列。

+ [新增] 画布支持按连线一键整理选中节点或全部节点，并提供批量对齐与等距排列。

+ [新增] 拖动节点时支持网格吸附和边缘、中心对齐辅助线，按住 Alt 可临时关闭吸附。

+ [修复] 左键点击或框选画布空白区域时关闭节点编辑框。

## v0.0.8 - 2026-07-14 - 原项目 v0.6.0

+ [调整] 画布平移改为仅按住鼠标中键触发，左键拖动空白区域改为框选节点。

+ [新增] 支持将选中节点及内部连线保存为节点组素材，并在画布中按原布局整体插入。

+ [调整] 前端正文和控件改用现代中文无衬线字体，页面标题保留宋体层级。

+ [优化] 重构首页、顶部导航和创作工作台视觉，统一蓝色主题与节点坐标语言，并优化生成工作台、素材卡和画布项目的信息密度。

+ [优化] 提升浅色与深色画布中默认节点的底色、边框和阴影层次，避免节点融入画布背景。

+ [修复] 移除会干扰浮层定位的全局动态降级，恢复加载、悬停与面板过渡动画。

+ [修复] 移除画布内容层的常驻 GPU 合成提示，避免节点文字在静止时仍被栅格化而显得模糊。

+ [修复] 所有本地媒体清理入口统一保留视频创作台记录引用的视频与参考素材，避免操作画布或素材时误删缓存。

## v0.0.7 - 2026-07-13 - 原项目 v0.6.0

+ [优化] 更新画布主题、节点与工具栏视觉，并减少缩放和平移过程中的状态更新开销。

+ [优化] 统一首页、导航、画布库、素材库与生成工作台的视觉语言和响应式布局。

## v0.0.6 - 2026-07-13 - 原项目 v0.6.0

+ [新增] Ray3.14 视频模型支持清晰度、比例、秒数与负面提示词控制。

+ [修复] 画布视频设置弹窗交互导致浮层意外关闭的问题。

+ [修复] 画布视频设置的自定义秒数弹窗层级遮挡问题。

+ [修复] 画布视频节点切换类型后仍使用旧模型的问题。

+ [修复] 视频创作台立即记录任务，并保留历史生成视频的本地缓存。

+ [优化] 视频生成最长等待时间统一延长至十分钟。

+ [优化] 视频创作台支持自定义视频秒数。

+ [修复] 视频生成适配 chatgpt2api 的 Firefly VEO 同步接口与参考图。

+ [优化] 同步 Gemini 生图规格、视频生成兼容、图片重试持久化、画布切图和输入法交互改进。

## v0.0.5 - 2026-07-11 - 原项目 v0.5.0

+ [优化] 画布生图新增独立的分辨率与宽高比分步选择，并新增 21:9 超宽比例。
+ [优化] 文本素材详情页和画布素材选择详情页增加编辑内容入口。

## v0.0.4 - 2026-07-05 - 原项目 v0.5.0

+ [优化] 应用打开后自动清理未被画布或素材引用的本地媒体缓存。
+ [优化] 本地版本记录标题显示对应的原项目版本号。
+ [修复] 版本更新弹窗拆分本地精简版和原项目更新记录，避免不同版本号混排。
+ [优化] 生图成功音效支持试听当前选择。
+ [优化] 生图成功音效支持显示和选择已上传的自定义音效。
+ [修复] 我的素材中文字素材过长会撑高卡片的问题。

## v0.0.3 - 2026-07-05 - 原项目 v0.5.0

+ [调整] 前端迁移到 Vite + React Router，开发和静态部署链路更轻量。
+ [调整] Docker 运行改为 nginx 静态托管，默认端口保持 3000。
+ [新增] 渠道兼容 Gemini 调用格式。
+ [调整] 精简个人版入口，移除本地 Agent / MCP、右侧画布助手、内置提示词库和应用内文档入口。


